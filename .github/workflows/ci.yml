name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Validate RDF
        uses: vemonet/jena-riot-action
        with:
          input: knowledge/Thing.ttl
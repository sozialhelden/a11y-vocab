name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Validate Thing.ttl
        uses: vemonet/jena-riot-action@v3.14
        with:
          input: knowledge/Thing.ttl,knowledge/Toilet.ttl
